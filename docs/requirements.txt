# Documentation build dependencies (MkDocs + Material theme).
#   pip install -r docs/requirements.txt
#   mkdocs serve      # live-reload local preview at http://127.0.0.1:8000
#   mkdocs build      # static site into ./site
mkdocs-material>=9.5
